confirmModulesPurge=false
node-linker=hoisted
symlink=false
# Allow build scripts for the packages explicitly listed in
# pnpm-workspace.yaml's `onlyBuiltDependencies`. Everything else is sandboxed.
# Equivalent to npm's `--ignore-scripts` for the un-allowlisted set.
strict-dep-builds=false
